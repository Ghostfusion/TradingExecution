# TradingExecution

Execution layer for the TradingAgents + TradingWeb stack.

Planned / placeholder — scaffolding only (no logic yet).

Layout
- `./` — execution engine (orders, fills, risk-exit hooks)
- `./.github/` — CI

See the parent workspace:
- `TradingAgents` — multi-agent LLM research framework (reports, decisions, signals)
- `trading_web` — FastAPI + React web app (jobs, screener, dashboards)
